# @fastify-resty/typeorm

The `TypeORM` connector for `fastify-resty` framework.

## Install

Using npm:

```sh
npm install @fastify-resty/typeorm
```

or using yarn:

```sh
yarn add @fastify-resty/typeorm
```
